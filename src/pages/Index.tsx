import { useDashboard, useAuth } from '@/contexts/DashboardContext';

// This page is not used - the app uses conditional rendering in App.tsx
// Keeping for potential future routing needs

const Index = () => {
  return null;
};

export default Index;
